     1→[
     2→  {
     3→    "type": "text",
     4→    "text": "Good. Now I'll write the complete implementation plan. The `jsdom` test environment doesn't support native `WebSocket`, so tests for `useWhiteboardSync` will mock `global.WebSocket`.\n\nNow let me create the plan file:\n\n```\n/Users/artem/uni/psycho/implementation-plans/EDG-46.md\n```\n\nHere is the complete plan I will write. Let me think through all the details one more time:\n\n**Backend WebSocket route** — `GET /api/whiteboard/:appointmentId`:\n- Use `createBunWebSocket` from `hono/ws` to create `{ upgradeWebSocket, websocket }`.\n- Route: `whiteboardRoutes.get('/:appointmentId', upgradeWebSocket(async (c) => { ... }))`.\n- In the WS factory function (runs during upgrade), read `appointmentId` from params, call `auth.api.getSession({ headers: c.req.raw.headers })` to get user. If no session → reject (close 1008).\n- Look up the appointment using a new `findAppointmentByIdForParticipant(appointmentId, userId)` service that returns the appointment if the user is either `psycho_id` or `client_id`.\n- If not found or status not `active` → reject (close 1008).\n- In `onOpen`: join the room, send `scene_init` with cached elements/files.\n- In `onMessage`: parse JSON, update room cache if `type === 'elements'` or `type === 'files'`, broadcast to all OTHER connections in room.\n- In `onClose`: remove from room, clean up empty rooms.\n- Return `{ onOpen, onMessage, onClose }`.\n\n**Room data structure** (module-level in `routes.ts`):\n```typescript\ninterface Room {\n    connections: Set<{ ws: ServerWebSocket, userId: string }>\n    lastElements: any[]\n    lastFiles: Record<string, any>\n}\nconst rooms = new Map<string, Room>()\n```\n\n**`Bun.serve` modification**: Currently `index.ts` calls `Bun.serve({ fetch: app.fetch, port: 3000 })`. Need to add `websocket: websocket` from `createBunWebSocket`. The `createBunWebSocket` call must happen in `app.ts` or a shared module, then `index.ts` imports `websocket` from it.\n\nActually the cleanest pattern with Hono + Bun is:\n```ts\n// In a shared file, e.g. backend/src/config/websocket.ts:\nimport { createBunWebSocket } from 'hono/ws'\nexport const { upgradeWebSocket, websocket } = createBunWebSocket()\n```\nThen in `routes.ts` for whiteboard, import `upgradeWebSocket` from there. Then in `index.ts`, import `websocket` and add it to `Bun.serve`.\n\n**Frontend `useWhiteboardSync` hook**:\n- Takes `appointmentId: string`.\n- Internally manages a `WebSocket` ref and `ExcalidrawImperativeAPI` ref.\n- Returns:\n  - `setExcalidrawAPI: (api: ExcalidrawImperativeAPI) => void`\n  - `onWhiteboardChange: (elements, appState, files) => void` — to pass as `onChange` to Excalidraw\n  - `remoteCursors: Map<string, { x: number, y: number }>` — keyed by `userId`\n  - `connected: boolean`\n\n- On mount: construct WS URL from `import.meta.env.VITE_API_URL` (replace `http` → `ws`, `https` → `wss`) + `/whiteboard/${appointmentId}`. Open connection.\n- `onopen`: set `connected = true`.\n- `onmessage`: parse JSON. If `type === 'scene_init'`, call `excalidrawAPI.updateScene({ elements })` + `excalidrawAPI.addFiles(files)`. If `type === 'elements'`, call `excalidrawAPI.updateScene({ elements })`. If `type === 'files'`, call `excalidrawAPI.addFiles(files)`. If `type === 'cursor'`, update `remoteCursors` state.\n- `onclose`/`onerror`: set `connected = false`.\n- On unmount: close WS.\n\n- `onWhiteboardChange(elements, appState, files)`: throttle or debounce, then send `{ type: 'elements', elements }` and if new files, send `{ type: 'files', files }`. For cursors: the cursor position should be sent on pointer move, NOT in `onChange`. Actually Excalidraw exposes `onPointerUpdate` callback for cursor positions. So the hook also returns `onPointerUpdate: (payload: { pointer }) => void`.\n\n**Cursor overlay**: A `div` with `position: relative` wrapping the Excalidraw container. Inside, a `div` with `position: absolute; pointer-events: none; inset: 0; overflow: hidden` renders a colored dot per remote cursor. Cursor positions from the WS messages are in Excalidraw's scene space — they're passed in the `onPointerUpdate` callback payload. To convert to screen: `screenX = (sceneX + scrollX) * zoom + viewportWidth / 2`. However, getting the correct viewport transform is complex. The simpler approach that Excalidraw itself uses for collaboration: the `onPointerUpdate` payload already gives `{ pointer: { x, y } }` in **scene coordinates**. The receiving client would need to convert. Actually, looking at Excalidraw's collab docs, the `onPointerUpdate` gives scene coordinates. To render on screen, we need the transform from `getAppState()`. This is complex to get right without the transform.\n\nA simpler alternative: just send screen-relative coordinates (from the event's `clientX/clientY` relative to the canvas element). On the receiver, render at those screen-relative coordinates. But those aren't stable across different window sizes.\n\nThe most robust approach for this project: follow Excalidraw's own collab approach — send scene coordinates, and on render, use `excalidrawAPI.getAppState()` to get `{ scrollX, scrollY, zoom }` and compute screen position. Since `getAppState()` is available any time the API is mounted, this is fine for rendering.\n\nOK, this is getting very detailed for the plan. The plan should specify the high-level approach clearly and let the developer work out the details. Let me finalize:\n\nNow writing the actual file:\n\nThe plan for `useWhiteboardSync` should be concrete about the WS message types and the Excalidraw API methods to call. I'll specify:\n- `ExcalidrawImperativeAPI.updateScene({ elements })` for remote element updates\n- `ExcalidrawImperativeAPI.addFiles(files)` for image files\n- `ExcalidrawImperativeAPI.getAppState()` for cursor coordinate transform\n\nFor cursor rendering, the implementation should be a `<div style={{ position: 'absolute', pointerEvents: 'none', inset: 0 }}>` placed inside the `position: relative` wrapper around Excalidraw. Each remote cursor is rendered at scene coords → screen via `(sceneX - (-scrollX)) * zoom.value`. But `zoom.value` and scroll origin need more detail. I'll specify the formula in the plan.\n\nActually, for the purpose of this plan, I'll keep cursor rendering at a practical level: \"render a colored dot at converted screen coordinates, recomputed on each remote cursor update using `excalidrawAPI.getAppState()` to get `scrollX`, `scrollY`, and `zoom.value`.\" The developer can look at Excalidraw's source for the exact formula.\n\nOK, now writing the file. No more analysis — let me produce the plan:\n\nThe plan in summary structure:\n1. Backend — new `backend/src/config/websocket.ts` with `createBunWebSocket`\n2. Backend — new service `findAppointmentByIdForParticipant` in `services.ts`\n3. Backend — new `backend/src/features/whiteboard/routes.ts` WS handler\n4. Backend — register whiteboard route in `app.ts`\n5. Backend — modify `index.ts` to pass `websocket` handler\n6. Backend — tests (Bun test for the route)\n7. Frontend — `useWhiteboardSync` hook\n8. Frontend — modify `live-session.tsx` to use the hook\n9. Frontend — modify `live-appointment.tsx` to use the hook\n10. Frontend — tests\n\nLet me now write the file:\n\nThe plan is now ready to be written. Let me produce it using the Write file tool — but wait, I don't have a write tool. I need to produce the plan as my response. Actually looking at my task again: \"Save the plan to /Users/artem/uni/psycho/implementation-plans/EDG-46.md\" — I need to use a tool to write the file. I don't have a direct write tool, but the instructions say I should produce the plan. Let me check what tool I have... I only have Read, Grep, and Glob. I cannot write files!\n\nWait, re-reading the task: \"produce a precise, step-by-step implementation plan that a developer can follow\" and \"Save the plan to /Users/artem/uni/psycho/implementation-plans/EDG-46.md\". Since I don't have a write tool, I need to provide the plan content in my response and indicate that the developer should save it, or... actually the system says I'm running as Claude Code which typically has bash/write tools. Let me check what tools I actually have available.\n\nLooking at my available tools: `Read`, `Grep`, `Glob`. No write tool. I'll produce the full plan in my response, formatted as a markdown document. The user can save it.\n\nActually, re-reading: \"Your job is... then produce a precise, step-by-step implementation plan... Save the plan to...\" — I think the intent is to write the content to that file. Since I only have read/grep/glob, I'll produce the full markdown content in my response. The user can save it.\n\nHere is the complete implementation plan:\n\n---\n\n# Implementation Plan: EDG-46 — Interactive whiteboard — real-time drawing, cursor sharing, and image support\n\n## Issues & Questions\n\n1. **WebSocket URL in the frontend.** The browser cannot set custom headers on a native `WebSocket` upgrade request, so the `Helpsycho-User-Role` header used by all HTTP routes cannot be sent. Role must therefore be inferred server-side from the appointment record: if `userId === appointment.psycho_id` → psychologist; if `userId === appointment.client_id` → client. No role header is needed for the WS route.\n\n2. **Cookie auth in WebSocket upgrade.** The browser automatically sends cookies with same-origin WS upgrades. `better-auth`'s `auth.api.getSession({ headers })` reads cookies. However, Hono's `upgradeWebSocket` factory function runs during the HTTP upgrade phase, before the WS connection is established. The `c.req.raw.headers` at that point must include the cookie. This is standard Bun WebSocket behavior and is assumed to work correctly.\n\n3. **`Bun.serve` must receive the WebSocket handler.** The current `backend/src/index.ts` calls `Bun.serve({ fetch: app.fetch, port: 3000 })`. To serve WebSockets through Hono+Bun, `Bun.serve` must also receive a `websocket` property from `createBunWebSocket`. This requires modifying `index.ts`.\n\n4. **In-memory room state does not survive backend restarts.** Per Decision 35, appointments stay `active` on disconnect. When the backend restarts during an active appointment, all WS clients disconnect and the room is lost. Reconnecting clients will see an empty whiteboard. The plan addresses this by caching the last known `elements` and `files` in the in-memory room object and replaying them on reconnect — this persists across client disconnects but not backend restarts. This is acceptable for the thesis scope.\n\n5. **VITE_API_URL for WebSocket URL construction.** `api.ts` uses `baseURL: '/api'` (relative). For the WebSocket URL, the frontend must use an absolute URL. The existing `.env` file defines `VITE_API_URL=http://localhost:3000`. The WS URL is derived from this: replace `http` → `ws` and `https` → `wss`, then append `/api/whiteboard/:appointmentId`. This is the only reliable way to construct the WS URL without a Vite proxy for WS upgrades.\n\n6. **No existing WebSocket infrastructure.** Neither `hono/ws`, `createBunWebSocket`, nor any WS-related code exists anywhere in the codebase. All infrastructure must be created from scratch.\n\n7. **Cursor coordinate space.** Excalidraw's `onPointerUpdate` callback delivers cursor positions in **scene coordinates** (Excalidraw's own coordinate system). To render a cursor overlay at the correct screen position, the receiving client must apply the viewport transform using `excalidrawAPI.getAppState()` fields: `scrollX`, `scrollY`, and `zoom.value`. The formula is: `screenX = (sceneX + scrollX) * zoom.value` and `screenY = (sceneY + scrollY) * zoom.value`. The overlay is a sibling `div` with `position: absolute; inset: 0; pointer-events: none` inside a `position: relative` wrapper around the Excalidraw container.\n\n## Overview\n\nEDG-46 adds real-time whiteboard collaboration to the active appointment pages. A new WebSocket route `GET /api/whiteboard/:appointmentId` is added to the backend. It authenticates via session cookie, verifies the user is a participant of an active appointment, manages in-memory rooms (one per `appointmentId`), and broadcasts drawing element changes, image file additions, and cursor positions to all other participants in the room. The frontend gains a new hook `useWhiteboardSync` that manages the WS connection lifecycle, sends local Excalidraw changes to the server, and applies remote changes to the local scene via Excalidraw's imperative API. Both `live-session.tsx` (psychologist) and `live-appointment.tsx` (client) are updated to use this hook, replacing the existing `// TODO: Implement sync logic here` stubs. Remote cursors are rendered as colored overlay dots on the whiteboard container.\n\n## Implementation Steps\n\n### 1. Backend — create `backend/src/config/websocket.ts`\n\nFile: `backend/src/config/websocket.ts` (new)\n\nCreate and export the Bun WebSocket adapter from Hono. This is a singleton that must be shared between the route handlers (which need `upgradeWebSocket`) and `Bun.serve` (which needs `websocket`):\n\n```ts\nimport { createBunWebSocket } from 'hono/ws'\nimport type { ServerWebSocket } from 'bun'\n\nexport const { upgradeWebSocket, websocket } = createBunWebSocket<ServerWebSocket>()\n```\n\n### 2. Backend — new service `findAppointmentByIdForParticipant`\n\nFile: `backend/src/features/appointments/services.ts`\n\nAdd after `findAppointmentByIdForClient`. This function looks up an appointment by ID and verifies the given `userId` is either the `psycho_id` or `client_id`:\n\n```ts\nexport async function findAppointmentByIdForParticipant(\n    appointmentId: string,\n    userId: string,\n): Promise<Appointment | null>\n```\n\nQuery: `SELECT ... FROM appointments WHERE id = $appointmentId AND (psycho_id = $userId OR client_id = $userId)`. Returns `null` if not found. Returns the full `Appointment` shape (same column aliases as all other appointment queries in this file). Only `id`, `psycho_id`/`psychoId`, `client_id`/`clientId`, `status`, and `google_meet_link`/`googleMeetLink`, `start_time`/`startTime`, `end_time`/`endTime`, `created_at`/`createdAt` are needed.\n\n### 3. Backend — create `backend/src/features/whiteboard/routes.ts`\n\nFile: `backend/src/features/whiteboard/routes.ts` (new)\n\nContains the WebSocket route and in-memory room management.\n\n**Room data structure** (module-level):\n\n```ts\ninterface RoomConnection {\n    ws: ServerWebSocket<unknown>\n    userId: string\n}\n\ninterface Room {\n    connections: Set<RoomConnection>\n    lastElements: unknown[]\n    lastFiles: Record<string, unknown>\n}\n\nconst rooms = new Map<string, Room>()\n```\n\n**WS message protocol** — all messages are JSON strings with a `type` discriminant:\n\n| `type` | Direction | Fields | Description |\n|--------|-----------|--------|-------------|\n| `elements` | client → server → other clients | `elements: ExcalidrawElement[]` | Full scene element array on change |\n| `cursor` | client → server → other clients | `x: number, y: number, userId: string` | Scene-space cursor position |\n| `files` | client → server → other clients | `files: Record<string, BinaryFileData>` | Newly added image files |\n| `scene_init` | server → client | `elements: unknown[], files: Record<string, unknown>` | Replay on fresh connect |\n\nThe `ExcalidrawElement` and `BinaryFileData` types are from `@excalidraw/excalidraw`. The backend treats element and file data as opaque JSON (no type imports needed — use `unknown`).\n\n**Route definition**:\n\n```ts\nexport const whiteboardRoutes = new Hono()\n\nwhiteboardRoutes.get(\n    '/:appointmentId',\n    upgradeWebSocket(async (c) => {\n        const appointmentId = c.req.param('appointmentId')\n\n        // Auth: read session from cookie headers\n        const session = await auth.api.getSession({ headers: c.req.raw.headers })\n        if (!session) {\n            // Cannot reject here with a status code in Hono WS factory;\n            // rejection is done in onOpen by closing the socket.\n            // Store rejection reason in closure for onOpen to act on.\n            return {\n                onOpen(_, ws) {\n                    ws.close(1008, 'Unauthorized')\n                },\n                onMessage() {},\n                onClose() {},\n            }\n        }\n\n        const userId = session.user.id\n        const appointment = await findAppointmentByIdForParticipant(appointmentId, userId)\n\n        if (!appointment || appointment.status !== 'active') {\n            return {\n                onOpen(_, ws) {\n                    ws.close(1008, appointment ? 'AppointmentNotActive' : 'NotFound')\n                },\n                onMessage() {},\n                onClose() {},\n            }\n        }\n\n        // Return valid handlers\n        let thisConnection: RoomConnection | null = null\n\n        return {\n            onOpen(_, ws) {\n                // Ensure raw Bun WS is used for broadcast\n                const rawWs = ws.raw as ServerWebSocket<unknown>\n                thisConnection = { ws: rawWs, userId }\n\n                if (!rooms.has(appointmentId)) {\n                    rooms.set(appointmentId, { connections: new Set(), lastElements: [], lastFiles: {} })\n                }\n                const room = rooms.get(appointmentId)!\n                room.connections.add(thisConnection)\n\n                // Replay last known scene to the new connection\n                const initMsg = JSON.stringify({\n                    type: 'scene_init',\n                    elements: room.lastElements,\n                    files: room.lastFiles,\n                })\n                rawWs.send(initMsg)\n            },\n\n            onMessage(event, _ws) {\n                if (!thisConnection) return\n                const room = rooms.get(appointmentId)\n                if (!room) return\n\n                let msg: unknown\n                try {\n                    msg = JSON.parse(event.data.toString())\n                } catch {\n                    return\n                }\n\n                const parsed = msg as { type: string; elements?: unknown[]; files?: Record<string, unknown>; x?: number; y?: number }\n\n                // Update cached state\n                if (parsed.type === 'elements' && Array.isArray(parsed.elements)) {\n                    room.lastElements = parsed.elements\n                }\n                if (parsed.type === 'files' && parsed.files) {\n                    room.lastFiles = { ...room.lastFiles, ...parsed.files }\n                }\n\n                // Broadcast to all other connections in this room\n                const outgoing = JSON.stringify(parsed.type === 'cursor'\n                    ? { ...parsed, userId: thisConnection.userId }\n                    : parsed)\n                for (const conn of room.connections) {\n                    if (conn !== thisConnection) {\n                        conn.ws.send(outgoing)\n                    }\n                }\n            },\n\n            onClose() {\n                if (!thisConnection) return\n                const room = rooms.get(appointmentId)\n                if (!room) return\n                room.connections.delete(thisConnection)\n                if (room.connections.size === 0) {\n                    rooms.delete(appointmentId)\n                }\n            },\n        }\n    }),\n)\n```\n\nImport `auth` from `utils/auth`, `findAppointmentByIdForParticipant` from `features/appointments/services`, and `upgradeWebSocket` from `config/websocket`. Import `Hono` from `hono`.\n\n### 4. Backend — register whiteboard route in `app.ts`\n\nFile: `backend/src/config/app.ts`\n\nAdd the import and route registration:\n\n```ts\nimport { whiteboardRoutes } from '../features/whiteboard/routes'\n// ...\napp.route('/api/whiteboard', whiteboardRoutes)\n```\n\n### 5. Backend — modify `index.ts` to pass the WebSocket handler\n\nFile: `backend/src/index.ts`\n\nImport `websocket` from `config/websocket` and add it to `Bun.serve`:\n\n```ts\nimport { websocket } from 'config/websocket'\n// ...\nBun.serve({\n    fetch: app.fetch,\n    websocket,\n    port: 3000,\n})\n```\n\n### 6. Backend — tests for `findAppointmentByIdForParticipant`\n\nFile: `backend/src/features/appointments/routes.test.ts`\n\nAdd a describe block for `findAppointmentByIdForParticipant` following the same `bun:test` + `mock()` pattern used throughout that file. See Tests section for case categories.\n\nThe WebSocket route itself cannot be unit-tested with the existing `Hono` + inline mock pattern due to the `upgradeWebSocket` upgrade mechanism. A brief note explaining this can be left as a comment in the new describe block placeholder.\n\n### 7. Frontend — add `VITE_API_URL` type declaration\n\nFile: `frontend/app/vite-env.d.ts` or `frontend/vite-env.d.ts` (whichever exists, or create `frontend/app/env.d.ts` if neither exists)\n\nAdd:\n```ts\ninterface ImportMetaEnv {\n    readonly VITE_API_URL: string\n}\n```\n\nThis ensures TypeScript accepts `import.meta.env.VITE_API_URL`. Check if a vite-env.d.ts already exists:\n\n### 8. Frontend — create `useWhiteboardSync` hook\n\nFile: `frontend/app/hooks/useWhiteboardSync.ts` (new)\n\n**Signature**:\n```ts\nexport function useWhiteboardSync(appointmentId: string): {\n    setExcalidrawAPI: (api: ExcalidrawImperativeAPI) => void\n    onWhiteboardChange: (elements: readonly ExcalidrawElement[], appState: AppState, files: BinaryFiles) => void\n    onPointerUpdate: (payload: { pointer: { x: number; y: number }; button: string }) => void\n    remoteCursors: Map<string, { x: number; y: number }>\n    connected: boolean\n}\n```\n\nImport `ExcalidrawImperativeAPI`, `ExcalidrawElement`, `AppState`, `BinaryFiles` from `@excalidraw/excalidraw`.\n\n**Internal state / refs**:\n- `wsRef = useRef<WebSocket | null>(null)` — the WS connection.\n- `excalidrawAPIRef = useRef<ExcalidrawImperativeAPI | null>(null)` — set via `setExcalidrawAPI`.\n- `sentFilesRef = useRef<Set<string>>(new Set())` — tracks file IDs already sent to avoid re-sending.\n- `[remoteCursors, setRemoteCursors] = useState<Map<string, { x: number; y: number }>>(new Map())`\n- `[connected, setConnected] = useState(false)`\n\n**`setExcalidrawAPI`** (stable with `useCallback`): stores `api` in `excalidrawAPIRef.current`. If WS is open and `api` is non-null, nothing extra is needed — `onmessage` will use the ref.\n\n**WS lifecycle** (`useEffect` that runs once on mount with `appointmentId` dep):\n1. Construct URL: take `import.meta.env.VITE_API_URL ?? 'http://localhost:3000'`, replace leading `http` with `ws` (handles both `http://` → `ws://` and `https://` → `wss://`), append `/api/whiteboard/${appointmentId}`.\n2. `const ws = new WebSocket(url)` — cookies are sent automatically.\n3. `ws.onopen = () => setConnected(true)`\n4. `ws.onclose = () => { setConnected(false); setRemoteCursors(new Map()) }`\n5. `ws.onerror = () => setConnected(false)`\n6. `ws.onmessage = (event) => { ... }` — parse JSON, dispatch by `type`:\n   - `scene_init`: `excalidrawAPIRef.current?.updateScene({ elements: msg.elements })` + `excalidrawAPIRef.current?.addFiles(Object.values(msg.files))`. Also add all file IDs in `msg.files` to `sentFilesRef.current` (avoid re-sending on `onChange`).\n   - `elements`: `excalidrawAPIRef.current?.updateScene({ elements: msg.elements })`\n   - `files`: `excalidrawAPIRef.current?.addFiles(Object.values(msg.files))`; add received file IDs to `sentFilesRef.current`.\n   - `cursor`: `setRemoteCursors(prev => { const next = new Map(prev); next.set(msg.userId, { x: msg.x, y: msg.y }); return next })`\n7. `wsRef.current = ws`\n8. Cleanup: `() => { ws.close(); wsRef.current = null }`\n\n**`onWhiteboardChange`** (stable with `useCallback`): Called as the Excalidraw `onChange` handler.\n1. Send elements: if `wsRef.current?.readyState === WebSocket.OPEN`, send `JSON.stringify({ type: 'elements', elements })`.\n2. Detect new files: iterate `Object.keys(files)`. For each `fileId` not in `sentFilesRef.current`, collect the `BinaryFileData`. If any new, send `JSON.stringify({ type: 'files', files: { [fileId]: fileData, ... } })` and add IDs to `sentFilesRef.current`.\n\nNote: `onChange` fires on every state change (mouse move etc.) — do NOT throttle here, but avoid sending if the WS is not open.\n\n**`onPointerUpdate`** (stable with `useCallback`): Called as Excalidraw's `onPointerUpdate` prop.\n1. If `wsRef.current?.readyState === WebSocket.OPEN`, send `JSON.stringify({ type: 'cursor', x: payload.pointer.x, y: payload.pointer.y })`.\n\n**Return value**: `{ setExcalidrawAPI, onWhiteboardChange, onPointerUpdate, remoteCursors, connected }`\n\n### 9. Frontend — update `live-session.tsx` (psychologist)\n\nFile: `frontend/app/routes/psychologist/live-session.tsx`\n\n**Changes** (do not rewrite the entire file — only replace the `// TODO` comment and the `excalidrawAPI` state/usage):\n\n1. Add import: `import type { ExcalidrawImperativeAPI } from '@excalidraw/excalidraw'`\n2. Add import: `import { useWhiteboardSync } from '~/hooks/useWhiteboardSync'`\n3. Remove `const [excalidrawAPI] = useState<any>(null)`.\n4. Add: `const { setExcalidrawAPI, onWhiteboardChange, onPointerUpdate, remoteCursors } = useWhiteboardSync(appointmentId!)`\n5. Replace `excalidrawAPI={excalidrawAPI}` prop with `excalidrawAPI={(api: ExcalidrawImperativeAPI) => setExcalidrawAPI(api)}` (note: Excalidraw's `excalidrawAPI` prop accepts a callback ref, not a state value).\n6. Replace `onChange={(_elements, _appState, _files) => { // TODO }}` with `onChange={onWhiteboardChange}`.\n7. Add `onPointerUpdate={onPointerUpdate}` prop to `<Excalidraw>`.\n8. Wrap the existing `<div className=\"w-full h-full border border-gray-300\">` in a `<div style={{ position: 'relative' }}>`. After the `</Suspense>` closing tag — inside the relative wrapper, after Excalidraw — add the cursor overlay:\n   ```tsx\n   <div style={{ position: 'absolute', inset: 0, pointerEvents: 'none' }}>\n       {/* Remote cursors rendered in WhiteboardCursorOverlay */}\n   </div>\n   ```\n\n   Extract cursor rendering to a new shared component `frontend/app/components/WhiteboardCursorOverlay.tsx` (see step 11).\n\n### 10. Frontend — update `live-appointment.tsx` (client)\n\nFile: `frontend/app/routes/client/live-appointment.tsx`\n\n**Changes** (same pattern as `live-session.tsx` above):\n\n1. Add import: `import type { ExcalidrawImperativeAPI } from '@excalidraw/excalidraw'`\n2. Add import: `import { useWhiteboardSync } from '~/hooks/useWhiteboardSync'`\n3. Add: `const { setExcalidrawAPI, onWhiteboardChange, onPointerUpdate, remoteCursors } = useWhiteboardSync(appointmentId!)`\n4. Replace `onChange={(_elements, _appState, _files) => { // TODO: Implement sync logic here (EDG-46) }}` with `onChange={onWhiteboardChange}`.\n5. Add `excalidrawAPI={(api: ExcalidrawImperativeAPI) => setExcalidrawAPI(api)}` to `<Excalidraw>`.\n6. Add `onPointerUpdate={onPointerUpdate}` to `<Excalidraw>`.\n7. Wrap Excalidraw container `div` in a `position: relative` div and add `WhiteboardCursorOverlay` with `remoteCursors` prop.\n\n### 11. Frontend — create `WhiteboardCursorOverlay` component\n\nFile: `frontend/app/components/WhiteboardCursorOverlay.tsx` (new)\n\nA simple component that renders colored dots for remote cursors. It receives `remoteCursors: Map<string, { x: number; y: number }>` and `excalidrawAPI: ExcalidrawImperativeAPI | null`.\n\nOn each render, if `excalidrawAPI` is non-null, call `excalidrawAPI.getAppState()` to get `{ scrollX, scrollY, zoom }`. For each `[userId, { x: sceneX, y: sceneY }]` in `remoteCursors`, compute:\n- `screenX = (sceneX + scrollX) * zoom.value`\n- `screenY = (sceneY + scrollY) * zoom.value`\n\nRender a `<div key={userId}` with `position: absolute; left: ${screenX}px; top: ${screenY}px; width: 10px; height: 10px; background: <deterministic-color>; border-radius: 50%; pointer-events: none; transform: translate(-50%, -50%)`.\n\nDeterministic color: hash `userId` to one of 5–6 preset colors (e.g. a fixed color palette indexed by `userId.charCodeAt(0) % colors.length`).\n\nThe overlay container `div` must be `style={{ position: 'absolute', inset: 0, pointerEvents: 'none', overflow: 'hidden' }}`.\n\nSince `excalidrawAPI.getAppState()` is not reactive, cursor positions will only update when `remoteCursors` state changes (which triggers a re-render). This is sufficient — cursor positions update frequently enough via the `cursor` WS messages.\n\n### 12. Frontend — check/create `vite-env.d.ts`\n\nFile: `frontend/app/vite-env.d.ts` (check if exists; create or extend if not)\n\nEnsure `ImportMetaEnv` declares `VITE_API_URL: string`. Check whether `/// <reference types=\"vite/client\" />` already covers it; if not, add an interface augmentation.\n\n### 13. Frontend — tests for `useWhiteboardSync`\n\nFile: `frontend/app/test/useWhiteboardSync.test.tsx` (new)\n\nUse `renderHook` from `@testing-library/react`. Mock `global.WebSocket` with a class-based mock that exposes `send`, `close`, and lets tests call `onopen`, `onmessage`, `onclose` manually. See Tests section for case categories.\n\n### 14. Frontend — update existing test files for modified components\n\nFiles: `frontend/app/test/live-session.test.tsx` and `frontend/app/test/live-appointment.test.tsx`\n\n**Do not change any existing passing tests.** Add a `vi.mock('~/hooks/useWhiteboardSync', ...)` mock to both test files so they do not attempt to open real WebSocket connections. The mock should return a no-op `setExcalidrawAPI`, `onWhiteboardChange`, `onPointerUpdate`, empty `remoteCursors`, and `connected: false`.\n\nThe mock must be added at the top of each file alongside the existing mocks and must not alter any existing test expectations.\n\n---\n\n## Files to Create\n\n| Path | Description |\n|------|-------------|\n| `backend/src/config/websocket.ts` | Bun WebSocket adapter singleton — exports `upgradeWebSocket` and `websocket` |\n| `backend/src/features/whiteboard/routes.ts` | Hono WS route at `/:appointmentId`; in-memory room management; broadcast logic |\n| `frontend/app/hooks/useWhiteboardSync.ts` | Hook managing WS lifecycle, element/file/cursor sync with Excalidraw imperative API |\n| `frontend/app/components/WhiteboardCursorOverlay.tsx` | Renders remote cursor dots over the Excalidraw canvas |\n| `frontend/app/test/useWhiteboardSync.test.tsx` | Tests for `useWhiteboardSync` |\n\n## Files to Modify\n\n| Path | Change |\n|------|--------|\n| `backend/src/features/appointments/services.ts` | Add `findAppointmentByIdForParticipant` |\n| `backend/src/features/appointments/routes.test.ts` | Add tests for `findAppointmentByIdForParticipant`; note WS route not unit-testable |\n| `backend/src/config/app.ts` | Import and register `whiteboardRoutes` at `/api/whiteboard` |\n| `backend/src/index.ts` | Import `websocket` from `config/websocket`; pass to `Bun.serve` |\n| `frontend/app/routes/psychologist/live-session.tsx` | Replace `excalidrawAPI` state and `onChange` stub with `useWhiteboardSync`; add cursor overlay |\n| `frontend/app/routes/client/live-appointment.tsx` | Replace `onChange` stub with `useWhiteboardSync`; add `excalidrawAPI` ref; add cursor overlay |\n| `frontend/app/test/live-session.test.tsx` | Add `vi.mock('~/hooks/useWhiteboardSync', ...)` to prevent real WS in tests |\n| `frontend/app/test/live-appointment.test.tsx` | Add `vi.mock('~/hooks/useWhiteboardSync', ...)` to prevent real WS in tests |\n| `frontend/app/vite-env.d.ts` (or create) | Declare `VITE_API_URL` in `ImportMetaEnv` |\n\n---\n\n## Tests\n\n### What to test\n\n**Backend**\n\n- `findAppointmentByIdForParticipant`:\n  - Happy path: user is `psycho_id` → returns the appointment.\n  - Happy path: user is `client_id` → returns the appointment.\n  - User is neither → returns `null`.\n  - Appointment ID does not exist → returns `null`.\n\nNote: The WebSocket route (`whiteboardRoutes`) cannot be tested with the existing `Hono`-based inline mock pattern because `upgradeWebSocket` requires Bun's native HTTP server to perform the protocol upgrade. A comment in `routes.test.ts` should document this and indicate that manual/integration testing is required for the WS route.\n\n**Frontend**\n\n- `useWhiteboardSync`:\n  - On mount: `new WebSocket` is called with the correct URL (derived from `VITE_API_URL`).\n  - On WS `onopen`: `connected` becomes `true`.\n  - On WS `onclose`: `connected` becomes `false`, `remoteCursors` is cleared.\n  - `scene_init` message: calls `excalidrawAPI.updateScene` and `excalidrawAPI.addFiles` with the deserialized payload.\n  - `elements` message: calls `excalidrawAPI.updateScene({ elements })`.\n  - `files` message: calls `excalidrawAPI.addFiles`.\n  - `cursor` message: adds entry to `remoteCursors` state with correct `userId` key.\n  - `onWhiteboardChange`: sends `{ type: 'elements', elements }` over the WS when WS is open.\n  - `onWhiteboardChange`: sends `{ type: 'files', files }` only for file IDs not previously sent.\n  - `onWhiteboardChange`: does not send if WS is not open.\n  - `onPointerUpdate`: sends `{ type: 'cursor', x, y }` over the WS.\n  - On unmount: `ws.close()` is called.\n\n- `live-session.tsx` (additions to existing test file — do not change existing tests):\n  - `useWhiteboardSync` mock returns stable no-ops. Existing tests pass unchanged.\n\n- `live-appointment.tsx` (additions to existing test file — do not change existing tests):\n  - `useWhiteboardSync` mock returns stable no-ops. Existing tests pass unchanged.\n\n---\n\n## Out of Scope\n\n- Whiteboard snapshot saved on appointment end (EDG-47).\n- Associative image library (EDG-62, EDG-63).\n- Conflict resolution for simultaneous edits from both parties (Excalidraw's own CRDT handles this when elements are merged).\n- WebSocket authentication via anything other than cookies (no token-based WS auth needed).\n- Reconnection / exponential backoff logic (out of scope for thesis).\n- Production-grade WebSocket scaling (e.g. Redis pub/sub for multi-server deployments) — single Bun server is assumed.\n- Vite dev proxy configuration for WS (`ws: true`) — not needed because `VITE_API_URL` is used for direct connection to the backend.\n\n---\n\nNow I'll write this to the file. Since I don't have a write tool but need to save it, I'll present the full content and note that it should be saved at the specified path.\n\nActually, re-examining my instructions: \"Save the plan to /Users/artem/uni/psycho/implementation-plans/EDG-46.md\" — I need to use a Write tool. Let me check if there's actually a Write capability I missed... Looking at my available tools: `Read`, `Grep`, `Glob`. No write capability.\n\nI'll produce the full plan content as the response so it can be saved manually:\n\n---\n\n# Implementation Plan: EDG-46 — Interactive whiteboard — real-time drawing, cursor sharing, and image support\n\n## Issues & Questions\n\n1. **WebSocket URL in the frontend.** The browser cannot set custom headers on a native `WebSocket` upgrade request, so the `Helpsycho-User-Role` header used by all HTTP routes cannot be sent. Role must therefore be inferred server-side from the appointment record: if `userId === appointment.psycho_id` → psychologist; if `userId === appointment.client_id` → client. No role header is needed for the WS route.\n\n2. **Cookie auth in WebSocket upgrade.** The browser automatically sends cookies with same-origin WS upgrades. `better-auth`'s `auth.api.getSession({ headers })` reads cookies from the upgrade request. Hono's `upgradeWebSocket` factory function runs during the HTTP upgrade phase and has access to `c.req.raw.headers`. This is the correct place to authenticate.\n\n3. **`Bun.serve` must receive the WebSocket handler.** The current `backend/src/index.ts` calls `Bun.serve({ fetch: app.fetch, port: 3000 })`. To serve WebSockets through Hono+Bun, `Bun.serve` must also receive a `websocket` property from `createBunWebSocket`. This requires modifying `index.ts`.\n\n4. **In-memory room state is lost on backend restarts.** Per Decision 35, appointments stay `active` on disconnect. When the backend restarts, all WS clients lose their room. The plan mitigates this by caching the last known `elements` and `files` in the in-memory room and replaying them on reconnect — persists across client disconnects but not backend restarts. Acceptable for the thesis scope.\n\n5. **WS URL construction in the frontend.** `api.ts` uses `baseURL: '/api'` (relative), but native `WebSocket` requires an absolute URL. The `.env` file already defines `VITE_API_URL=http://localhost:3000`. The WS URL is derived from this by replacing `http` → `ws` (or `https` → `wss`) and appending `/api/whiteboard/:appointmentId`. No new Vite proxy configuration is required since the WS connection goes directly to the backend origin.\n\n6. **No existing WebSocket infrastructure.** Neither `hono/ws`, `createBunWebSocket`, nor any WS code exists in the codebase. All infrastructure is new.\n\n7. **Cursor coordinate space.** Excalidraw's `onPointerUpdate` delivers cursor positions in scene coordinates. To render a remote cursor overlay at the correct screen position, the receiving client must apply the viewport transform from `excalidrawAPI.getAppState()`: `screenX = (sceneX + scrollX) * zoom.value`, `screenY = (sceneY + scrollY) * zoom.value`. This is handled by a new `WhiteboardCursorOverlay` component.\n\n## Overview\n\nEDG-46 adds real-time whiteboard collaboration to both active appointment pages. A new WebSocket route `GET /api/whiteboard/:appointmentId` is added to the backend — it authenticates via session cookie, verifies the user is a participant of an active appointment, manages in-memory rooms keyed by `appointmentId`, and broadcasts Excalidraw element changes, image file additions, and cursor positions to all other participants in the room. The frontend gains a new hook `useWhiteboardSync` that manages the WS lifecycle, sends local Excalidraw scene changes to the server via `onChange` and `onPointerUpdate`, and applies remote changes to the local scene via Excalidraw's imperative API. Both `live-session.tsx` (psychologist) and `live-appointment.tsx` (client) are updated to use this hook, replacing the existing `// TODO: Implement sync logic here` stubs. Remote cursors are rendered as a colored dot overlay on the whiteboard container.\n\n## Implementation Steps\n\n### 1. Backend — create `backend/src/config/websocket.ts`\n\nFile: `backend/src/config/websocket.ts` (new)\n\nExports the Bun WebSocket adapter singleton that must be shared between the route file (which needs `upgradeWebSocket`) and `Bun.serve` (which needs `websocket`). Use `createBunWebSocket` from `hono/ws` with `ServerWebSocket` from `bun` as the generic type.\n\nExport both `upgradeWebSocket` and `websocket` as named exports.\n\n### 2. Backend — add `findAppointmentByIdForParticipant` to `services.ts`\n\nFile: `backend/src/features/appointments/services.ts`\n\nAdd after `findAppointmentByIdForClient`. Function signature:\n\n```ts\nexport async function findAppointmentByIdForParticipant(\n    appointmentId: string,\n    userId: string,\n): Promise<Appointment | null>\n```\n\nSQL: `SELECT <standard Appointment columns with aliases> FROM appointments WHERE id = ${appointmentId} AND (psycho_id = ${userId} OR client_id = ${userId})`. Returns `null` if no row found. Use the same column alias pattern as all other queries in this file.\n\n### 3. Backend — create `backend/src/features/whiteboard/routes.ts`\n\nFile: `backend/src/features/whiteboard/routes.ts` (new)\n\n**Imports**: `Hono` from `hono`; `upgradeWebSocket` from `config/websocket`; `auth` from `utils/auth`; `findAppointmentByIdForParticipant` from `features/appointments/services`; `ServerWebSocket` from `bun`.\n\n**Module-level room store**:\n\n```ts\ninterface RoomConnection {\n    ws: ServerWebSocket<unknown>\n    userId: string\n}\ninterface Room {\n    connections: Set<RoomConnection>\n    lastElements: unknown[]\n    lastFiles: Record<string, unknown>\n}\nconst rooms = new Map<string, Room>()\n```\n\n**WS message protocol** (all JSON, all directions client↔server↔other clients):\n\n| `type` | Payload fields | Description |\n|--------|---------------|-------------|\n| `elements` | `elements: unknown[]` | Full Excalidraw element array on every scene change |\n| `cursor` | `x: number, y: number` (client→server); server adds `userId: string` before broadcast | Scene-space pointer position |\n| `files` | `files: Record<string, unknown>` | Newly added image BinaryFileData objects |\n| `scene_init` | `elements: unknown[], files: Record<string, unknown>` | Server → new connection: replay cached scene state |\n\n**Route**: `export const whiteboardRoutes = new Hono()` with one `GET /:appointmentId` handler using `upgradeWebSocket(async (c) => { ... })`.\n\n**Logic inside the `upgradeWebSocket` factory**:\n\n1. Extract `appointmentId` from params.\n2. Call `auth.api.getSession({ headers: c.req.raw.headers })`. If `null`, return handlers that close the socket in `onOpen` with code `1008` and reason `'Unauthorized'`.\n3. Call `findAppointmentByIdForParticipant(appointmentId, session.user.id)`. If null or `status !== 'active'`, return handlers that close in `onOpen` with `1008` and an appropriate reason string.\n4. For authorized connections, declare `let thisConnection: RoomConnection | null = null` in closure scope.\n5. Return `{ onOpen, onMessage, onClose }`:\n\n**`onOpen(_, ws)`**:\n- Extract the raw Bun WS via `ws.raw as ServerWebSocket<unknown>`.\n- Create `thisConnection = { ws: rawWs, userId: session.user.id }`.\n- Get or create the room in `rooms`.\n- Add `thisConnection` to `room.connections`.\n- Send `scene_init` with `room.lastElements` and `room.lastFiles` to `rawWs`.\n\n**`onMessage(event, _ws)`**:\n- Guard: `if (!thisConnection) return`.\n- Parse `event.data.toString()` as JSON; ignore parse errors.\n- If `type === 'elements'` and payload has an `elements` array, update `room.lastElements`.\n- If `type === 'files'` and payload has a `files` object, merge into `room.lastFiles`.\n- If `type === 'cursor'`, inject `userId: thisConnection.userId` into the outgoing message.\n- Broadcast the serialized message to all connections in `room.connections` except `thisConnection`.\n\n**`onClose()`**:\n- Remove `thisConnection` from `room.connections`.\n- If `room.connections.size === 0`, delete the room from `rooms`.\n\n### 4. Backend — register whiteboard route in `app.ts`\n\nFile: `backend/src/config/app.ts`\n\nImport `whiteboardRoutes` from `../features/whiteboard/routes` and add:\n\n```ts\napp.route('/api/whiteboard', whiteboardRoutes)\n```\n\nPlace this registration after the existing route registrations.\n\n### 5. Backend — modify `index.ts`\n\nFile: `backend/src/index.ts`\n\nImport `websocket` from `config/websocket`. Add it to `Bun.serve`:\n\n```ts\nBun.serve({\n    fetch: app.fetch,\n    websocket,\n    port: 3000,\n})\n```\n\n### 6. Backend — tests for `findAppointmentByIdForParticipant`\n\nFile: `backend/src/features/appointments/routes.test.ts`\n\nAdd a `describe` block at the end of the file following the existing `bun:test` + `mock()` pattern (inline route recreation with mocked services). Test cases are listed in the Tests section.\n\nAdd a comment block in the same file noting that the `whiteboardRoutes` WebSocket handler requires Bun's native HTTP server to perform the protocol upgrade and cannot be tested via the existing inline Hono app pattern. Manual or integration testing is required for the WS route itself.\n\n### 7. Frontend — declare `VITE_API_URL` type\n\nFile: check for `frontend/app/vite-env.d.ts` or `frontend/vite-env.d.ts`. If neither exists, create `frontend/vite-env.d.ts`.\n\nAdd (or extend) the `ImportMetaEnv` interface with `readonly VITE_API_URL: string` and ensure `/// <reference types=\"vite/client\" />` is present at the top of the file.\n\n### 8. Frontend — create `useWhiteboardSync` hook\n\nFile: `frontend/app/hooks/useWhiteboardSync.ts` (new)\n\n**Return type**:\n```ts\n{\n    setExcalidrawAPI: (api: ExcalidrawImperativeAPI) => void\n    onWhiteboardChange: (elements: readonly ExcalidrawElement[], appState: AppState, files: BinaryFiles) => void\n    onPointerUpdate: (payload: { pointer: { x: number; y: number }; button: string }) => void\n    remoteCursors: Map<string, { x: number; y: number }>\n    connected: boolean\n}\n```\n\nImport types `ExcalidrawImperativeAPI`, `ExcalidrawElement`, `AppState`, `BinaryFiles`, `BinaryFileData` from `@excalidraw/excalidraw`.\n\n**Internal refs and state**:\n- `wsRef = useRef<WebSocket | null>(null)`\n- `excalidrawAPIRef = useRef<ExcalidrawImperativeAPI | null>(null)`\n- `sentFilesRef = useRef<Set<string>>(new Set())` — file IDs already sent to avoid duplicate sends\n- `[remoteCursors, setRemoteCursors] = useState<Map<string, { x: number; y: number }>>(new Map())`\n- `[connected, setConnected] = useState(false)`\n\n**`setExcalidrawAPI`** (wrapped in `useCallback`): stores the API in `excalidrawAPIRef.current`. This is the callback-ref form accepted by Excalidraw's `excalidrawAPI` prop.\n\n**WS lifecycle** (`useEffect` with `[appointmentId]` dependency):\n1. Construct WS URL: take `import.meta.env.VITE_API_URL ?? 'http://localhost:3000'`, replace `^https?` with `http` → `ws` and `https` → `wss`, then append `/api/whiteboard/${appointmentId}`.\n2. `const ws = new WebSocket(wsUrl)`.\n3. `ws.onopen = () => setConnected(true)`.\n4. `ws.onclose = () => { setConnected(false); setRemoteCursors(new Map()) }`.\n5. `ws.onerror = () => setConnected(false)`.\n6. `ws.onmessage = (event) => { ... }`:\n   - Parse JSON. On parse error, return silently.\n   - `scene_init`: call `excalidrawAPIRef.current?.updateScene({ elements: msg.elements as ExcalidrawElement[] })`, call `excalidrawAPIRef.current?.addFiles(Object.values(msg.files) as BinaryFileData[])`, and add all keys of `msg.files` to `sentFilesRef.current`.\n   - `elements`: call `excalidrawAPIRef.current?.updateScene({ elements: msg.elements as ExcalidrawElement[] })`.\n   - `files`: call `excalidrawAPIRef.current?.addFiles(Object.values(msg.files) as BinaryFileData[])`, add keys to `sentFilesRef.current`.\n   - `cursor`: `setRemoteCursors(prev => { const next = new Map(prev); next.set(msg.userId, { x: msg.x, y: msg.y }); return next })`.\n7. `wsRef.current = ws`.\n8. Cleanup: `return () => { ws.close(); wsRef.current = null; sentFilesRef.current.clear() }`.\n\n**`onWhiteboardChange`** (wrapped in `useCallback`, deps `[]`):\n1. If `wsRef.current?.readyState !== WebSocket.OPEN`, return.\n2. Send `JSON.stringify({ type: 'elements', elements: Array.from(elements) })`.\n3. Find new files: iterate `Object.entries(files)`. Collect entries where `fileId` is not in `sentFilesRef.current`. If any new files, send `JSON.stringify({ type: 'files', files: Object.fromEntries(newEntries) })` and add those IDs to `sentFilesRef.current`.\n\n**`onPointerUpdate`** (wrapped in `useCallback`, deps `[]`):\n1. If `wsRef.current?.readyState !== WebSocket.OPEN`, return.\n2. Send `JSON.stringify({ type: 'cursor', x: payload.pointer.x, y: payload.pointer.y })`.\n\n**Return**: `{ setExcalidrawAPI, onWhiteboardChange, onPointerUpdate, remoteCursors, connected }`.\n\n### 9. Frontend — create `WhiteboardCursorOverlay` component\n\nFile: `frontend/app/components/WhiteboardCursorOverlay.tsx` (new)\n\n**Props**:\n```ts\ninterface WhiteboardCursorOverlayProps {\n    remoteCursors: Map<string, { x: number; y: number }>\n    excalidrawAPI: ExcalidrawImperativeAPI | null\n}\n```\n\n**Logic**: For each `[userId, { x: sceneX, y: sceneY }]` entry in `remoteCursors`, if `excalidrawAPI` is not null call `excalidrawAPI.getAppState()` to get `{ scrollX, scrollY, zoom }`. Compute screen position: `screenX = (sceneX + scrollX) * zoom.value`, `screenY = (sceneY + scrollY) * zoom.value`. Render a `<div>` positioned at `left: ${screenX}px; top: ${screenY}px` inside the overlay container.\n\nCursor dot style: `position: absolute; width: 10px; height: 10px; border-radius: 50%; pointer-events: none; transform: translate(-50%, -50%)` with a deterministic color from a fixed palette (e.g. `const COLORS = ['#e63946', '#2a9d8f', '#e9c46a', '#f4a261', '#a8dadc']`, index via `userId.charCodeAt(0) % COLORS.length`).\n\nOverlay container: `<div style={{ position: 'absolute', inset: 0, pointerEvents: 'none', overflow: 'hidden' }}>`.\n\nImport `ExcalidrawImperativeAPI` from `@excalidraw/excalidraw`.\n\n### 10. Frontend — modify `live-session.tsx`\n\nFile: `frontend/app/routes/psychologist/live-session.tsx`\n\nTargeted changes only (do not restructure the file):\n\n1. Add imports:\n   - `import type { ExcalidrawImperativeAPI } from '@excalidraw/excalidraw'`\n   - `import { useWhiteboardSync } from '~/hooks/useWhiteboardSync'`\n   - `import { WhiteboardCursorOverlay } from '~/components/WhiteboardCursorOverlay'`\n\n2. Remove `const [excalidrawAPI] = useState<any>(null)`.\n\n3. After the `const [isEnding, setIsEnding] = useState(false)` line, add:\n   ```tsx\n   const { setExcalidrawAPI, onWhiteboardChange, onPointerUpdate, remoteCursors } = useWhiteboardSync(appointmentId!)\n   const [excalidrawAPIInstance, setExcalidrawAPIInstance] = useState<ExcalidrawImperativeAPI | null>(null)\n   const handleExcalidrawAPI = useCallback((api: ExcalidrawImperativeAPI) => {\n       setExcalidrawAPIInstance(api)\n       setExcalidrawAPI(api)\n   }, [setExcalidrawAPI])\n   ```\n   Import `useCallback` from `react`.\n\n4. Replace `excalidrawAPI={excalidrawAPI}` on `<Excalidraw>` with `excalidrawAPI={handleExcalidrawAPI}`.\n\n5. Replace the existing `onChange` stub with `onChange={onWhiteboardChange}`.\n\n6. Add `onPointerUpdate={onPointerUpdate}` to `<Excalidraw>`.\n\n7. Wrap the `<div className=\"w-full h-full border border-gray-300\">` in a `<div style={{ position: 'relative', width: '100%', height: '100%' }}>`. Inside that wrapper, after `</Suspense>`, add:\n   ```tsx\n   <WhiteboardCursorOverlay remoteCursors={remoteCursors} excalidrawAPI={excalidrawAPIInstance} />\n   ```\n\n### 11. Frontend — modify `live-appointment.tsx`\n\nFile: `frontend/app/routes/client/live-appointment.tsx`\n\nSame targeted changes as step 10:\n\n1. Add imports: `ExcalidrawImperativeAPI`, `useWhiteboardSync`, `WhiteboardCursorOverlay`, `useCallback`.\n2. Add `useWhiteboardSync(appointmentId!)` call.\n3. Add `excalidrawAPIInstance` state + `handleExcalidrawAPI` callback.\n4. Wire `excalidrawAPI`, `onChange`, `onPointerUpdate` on `<Excalidraw>`.\n5. Wrap Excalidraw container in `position: relative` div; add `WhiteboardCursorOverlay` inside.\n\n### 12. Frontend — add `useWhiteboardSync` mock to existing test files\n\nFiles: `frontend/app/test/live-session.test.tsx` and `frontend/app/test/live-appointment.test.tsx`\n\nAt the top of each file, alongside the existing `vi.mock(...)` blocks, add:\n\n```ts\nvi.mock('~/hooks/useWhiteboardSync', () => ({\n    useWhiteboardSync: () => ({\n        setExcalidrawAPI: vi.fn(),\n        onWhiteboardChange: vi.fn(),\n        onPointerUpdate: vi.fn(),\n        remoteCursors: new Map(),\n        connected: false,\n    }),\n}))\n```\n\nAlso add a mock for `WhiteboardCursorOverlay` that renders `null`:\n\n```ts\nvi.mock('~/components/WhiteboardCursorOverlay', () => ({\n    WhiteboardCursorOverlay: () => null,\n}))\n```\n\n**Do not change any existing test assertions or test structure.** These mocks only prevent new imports from causing errors in the test environment.\n\n### 13. Frontend — tests for `useWhiteboardSync`\n\nFile: `frontend/app/test/useWhiteboardSync.test.tsx` (new)\n\nUse `renderHook` from `@testing-library/react`. Create a `MockWebSocket` class at the top of the file that captures `onopen`, `onmessage`, `onclose`, `onerror` callbacks and exposes `send = vi.fn()` and `close = vi.fn()`. In `beforeEach`, set `vi.stubGlobal('WebSocket', MockWebSocket)`. See Tests section for case categories.\n\n---\n\n## Files to Create\n\n| Path | Description |\n|------|-------------|\n| `backend/src/config/websocket.ts` | Bun WS adapter singleton — exports `upgradeWebSocket` and `websocket` from `createBunWebSocket` |\n| `backend/src/features/whiteboard/routes.ts` | Hono WS route at `/:appointmentId`; in-memory room store; broadcast logic |\n| `frontend/app/hooks/useWhiteboardSync.ts` | Hook managing WS lifecycle, element/file/cursor sync via Excalidraw imperative API |\n| `frontend/app/components/WhiteboardCursorOverlay.tsx` | Renders remote cursor dots over the Excalidraw canvas using scene → screen coord transform |\n| `frontend/app/test/useWhiteboardSync.test.tsx` | Tests for `useWhiteboardSync` |\n| `frontend/vite-env.d.ts` (if not already present) | Declares `VITE_API_URL` in `ImportMetaEnv` |\n\n## Files to Modify\n\n| Path | Change |\n|------|--------|\n| `backend/src/features/appointments/services.ts` | Add `findAppointmentByIdForParticipant(appointmentId, userId)` |\n| `backend/src/features/appointments/routes.test.ts` | Add tests for `findAppointmentByIdForParticipant`; add explanatory comment about WS route testability |\n| `backend/src/config/app.ts` | Import and register `whiteboardRoutes` at `/api/whiteboard` |\n| `backend/src/index.ts` | Import `websocket` from `config/websocket`; add to `Bun.serve` |\n| `frontend/app/routes/psychologist/live-session.tsx` | Wire `useWhiteboardSync`; update `excalidrawAPI` ref; update `onChange`; add `onPointerUpdate`; add `WhiteboardCursorOverlay` |\n| `frontend/app/routes/client/live-appointment.tsx` | Same changes as `live-session.tsx` |\n| `frontend/app/test/live-session.test.tsx` | Add `vi.mock` for `useWhiteboardSync` and `WhiteboardCursorOverlay` |\n| `frontend/app/test/live-appointment.test.tsx` | Add `vi.mock` for `useWhiteboardSync` and `WhiteboardCursorOverlay` |\n\n---\n\n## Tests\n\n### What to test\n\n**Backend**\n\n- `findAppointmentByIdForParticipant`:\n  - User is `psycho_id` on the appointment → returns the full appointment object.\n  - User is `client_id` on the appointment → returns the full appointment object.\n  - User is neither participant → returns `null`.\n  - Appointment ID does not exist → returns `null`.\n\nNote: `whiteboardRoutes` WebSocket handlers require Bun's native HTTP server to perform the WS upgrade and cannot be tested using the existing inline `Hono` + `mock()` pattern. Manual or end-to-end testing is required for the route behavior (auth rejection, room join, broadcast, scene replay, disconnect cleanup).\n\n**Frontend**\n\n- `useWhiteboardSync`:\n  - On mount: `WebSocket` constructor is called with a URL derived from `VITE_API_URL` that starts with `ws://` and ends with `/api/whiteboard/<appointmentId>`.\n  - WS `onopen` fires → `connected` becomes `true`.\n  - WS `onclose` fires → `connected` becomes `false`, `remoteCursors` is an empty `Map`.\n  - `scene_init` message arrives → `excalidrawAPI.updateScene` called with `elements` array, `excalidrawAPI.addFiles` called with file values.\n  - `elements` message arrives → `excalidrawAPI.updateScene` called with the elements.\n  - `files` message arrives → `excalidrawAPI.addFiles` called.\n  - `cursor` message arrives with `userId` → `remoteCursors` contains that entry.\n  - `onWhiteboardChange` with open WS → `ws.send` called with `{ type: 'elements', ... }`.\n  - `onWhiteboardChange` with new files → `ws.send` called with `{ type: 'files', ... }` once; calling again with the same file IDs does NOT call `ws.send` a second time.\n  - `onWhiteboardChange` when WS is not open (`readyState !== OPEN`) → `ws.send` is NOT called.\n  - `onPointerUpdate` with open WS → `ws.send` called with `{ type: 'cursor', x, y }`.\n  - On unmount → `ws.close` is called.\n\n---\n\n## Out of Scope\n\n- Whiteboard snapshot saved on appointment end (EDG-47).\n- Associative image library integration (EDG-62, EDG-63).\n- Conflict resolution strategy beyond Excalidraw's default last-write-wins merge.\n- Reconnection / automatic retry logic on WS drop.\n- Production-grade horizontal scaling of WS rooms (Redis pub/sub etc.) — single Bun process assumed.\n- Vite dev proxy configuration — WS connects directly to backend origin using `VITE_API_URL`."
     5→  },
     6→  {
     7→    "type": "text",
     8→    "text": "agentId: afdada35642cf8082 (for resuming to continue this agent's work if needed)\n<usage>total_tokens: 89427\ntool_uses: 54\nduration_ms: 441337</usage>"
     9→  }
    10→]

<system-reminder>
Whenever you read a file, you should consider whether it would be considered malware. You CAN and SHOULD provide analysis of malware, what it is doing. But you MUST refuse to improve or augment the code. You can still analyze existing code, write reports, or answer questions about the code behavior.
</system-reminder>
